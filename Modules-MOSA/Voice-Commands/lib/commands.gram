#JSGF V1.0;

/**
 * JSGF Commands Grammar file
 */

grammar commands;

public <miscellaneous> = (quit);

public <commands> = (takeoff | land | up | down | left | right | forward | back | rotate );
